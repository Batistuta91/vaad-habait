node_modules/
.env
npm-debug.log*

# data.json IS committed to git so settings survive deploys.
# If you add a Railway Volume at /data, data.json there takes priority
# and this file becomes a fallback seed only.
