# 🚂 הגדרת Railway — שמירת נתונים קבועה

## הבעיה
בכל deploy חדש ב-Railway, קבצים שנשמרו בתוך תיקיית הפרויקט **נמחקים**.
הפתרון: להוסיף **Volume** (אחסון קבוע) שמאוחסן ב-`/data`.

## שלבים להגדרה (חד פעמי)

1. נכנסים לפרויקט ב-Railway
2. לוחצים על השירות (`vaad-habait`)
3. לוחצים על **"+ Add Volume"** (בצד שמאל, מתחת לכפתור Deploy)
4. **Mount Path:** `/data`
5. לוחצים **Add**
6. עושים **Deploy** מחדש

זהו! מעכשיו כל הנתונים (תשלומים, הכנסות, הוצאות, יתרת פתיחה) **נשמרים לצמיתות** גם אחרי deploy חדש.

## אם לא ניתן להוסיף Volume (תוכנית חינמית)
ב-Railway תוכנית Hobby (בתשלום) תומכת ב-Volumes.
לחלופין, ניתן לשדרג ל-Hobby ($5/חודש) כדי לקבל אחסון קבוע.

## בדיקה
לאחר הגדרת ה-Volume ו-Deploy, הזן ביתרת הפתיחה סכום כלשהו, שמור, עשה Deploy נוסף — הנתון צריך להישמר.
